version: '3.8'

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=${NODE_ENV:-production}
    volumes:
      - .:/app
      - /app/node_modules
    command: yarn run ${NODE_ENV:-start}
    restart: always
